# Ralph Progress Log
Milestone: basic-functionality
Started: Tue 24 Mar 2026 14:22:29 CET
---

## Codebase Patterns
- feedparser enclosures: check `entry.links` for `rel="enclosure"` first, fallback to `entry.enclosures[0]`
- `utils.define_string_tokens()` uses `entry.get()` dict-style — feedparser entries support this
- Filename sanitization: replace `/ \ : * ? " < > |` with `_` before writing to disk
- `files.write_image()` works for any binary data, not just images
---

## [2026-03-24] - #1 Fix bugs in existing code
- Fixed duplicate dict keys in `utils.py` `define_string_tokens()`
- Fixed `files.py` `write_dir()` — `os.makedirs()` returns None, not truthy
- Fixed `subscribe.py` `get_feed_image_url()` — returned function ref instead of URL string; added `hasattr` guards for safety
- Files changed: `src/utils.py`, `src/files.py`, `src/subscribe.py`
- **Learnings for future iterations:**
  - Use `python3` not `python` on this system
  - `feedparser` `meta.image` can be either a string or an object with `.href` — always check with `isinstance` and `hasattr`
  - `os.makedirs()` always returns None — never use it in a conditional
---

## [2026-03-24] - #3 Download podcast episode audio files
- Implemented episode download loop in `subscribe.py`
- Fixed `utils.replace_string_tokens()` to use `{key}` pattern and handle None values
- Enclosure detection: checks `entry.links` (rel=enclosure) then `entry.enclosures`
- Extension derived from URL filename or content-type header mapping
- Filename sanitized for filesystem safety
- Files changed: `src/subscribe.py`, `src/utils.py`
- **Learnings for future iterations:**
  - `utils.define_string_tokens()` uses `entry.get()` — works because feedparser entries support dict-style access
  - Extension extraction: split on `?` first to strip query params, then get last `.`-separated part
  - `{title}` token can contain slashes on some feeds — always sanitize filenames
---
## [2026-03-24] - #4 Save per-episode metadata and artwork
- Added episode metadata JSON saving after each audio download
- Added episode artwork downloading (entry.image.href or entry.links with image/ type)
- Skips if JSON/artwork already exists
- Files changed: `src/subscribe.py`
- **Learnings for future iterations:**
  - `json.dump()` returns None — `files.write_json()` always returns None, use `os.path.exists()` to check before writing
  - Episode image: check `entry.image.href` first, then scan `entry.links` for type starting with `image/`
  - Use `os.path.splitext(filepath)[0]` to get stem for sibling files
---
## [2026-03-24] - #5 Generate RSS XML output feeds (separate mode)
- Implemented `src/publish.py` with RSS 2.0 XML generation using stdlib `xml.etree.ElementTree`
- Scans each feed directory for episode JSON files, pairs with audio files for enclosure URLs
- Episodes sorted by publish date (newest first) via `parse_date()` helper
- `generate.type: false` skips generation; defaults to `"separate"` mode
- Updated `__main__.py` to import and call `publish.main(args[0])`
- Files changed: `src/publish.py`, `src/__main__.py`
- **Learnings for future iterations:**
  - Episode JSON files are named same as audio file stem (e.g., `episode.json` for `episode.mp3`)
  - Skip `feed.json` and `original.json` when scanning for episode files
  - `enclosure` URL uses format `/<feed_id>/<filename>` (relative, for future HTTP server)
  - `feeds/` subdirectory must be created before writing XML files
---
## [2026-03-24] - #6 Generate combined and custom named feeds
- Added `generate_combined()` for `generate.type: combined` → single `feeds/feeds.xml`
- Added `generate_custom_feeds()` for `generate.feeds[]` → one `<id>.xml` per custom feed
- Added `generate_default_all()` → always writes `feeds/feeds.xml` in separate mode
- Custom feeds generated regardless of `generate.type` (even `false`)
- Files changed: `src/publish.py`
- **Learnings for future iterations:**
  - `generate_cfg.get('type', 'separate')` can return `None` if key is present but empty — cast defensively
  - `generate.feeds` is a list of dicts with `name`, `id`, `feeds` (list of input feed IDs)
  - `feeds.xml` is the default all-episodes feed referenced by README at `/feeds.xml`
---
