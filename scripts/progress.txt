# Ralph Progress Log
Milestone: service-split
Started: Tue 24 Mar 2026 17:57:58 CET
---

## Codebase Patterns
- Use `python3` not `python` (system python3 is 3.14)
- Use `.venv/bin/pytest` to run tests (venv at project root)
- 7 pre-existing test failures exist before any changes (test_bootstrap, test_file_format_integration, test_subscribe)
- `config.file()` returns YAML dict; pass it to interval helpers as `cfg`
- Tests can be run with: `.venv/bin/pytest tests/ --ignore=tests/test_e2e_docker.py`

## [2026-03-24] - #22 Update README with two-service architecture
- Updated Installation, Schedule, Development sections; added Architecture section
- Files changed: `README.md`
- **Learnings:** No surprises; straightforward doc update
---

## [2026-03-24] - #21 Update docker-compose files with two services and three volumes
- Rewrote both compose files with subscriber/generator services and three volumes
- Updated mock/feeds.yaml path from /podcasts to /
- config/feeds.yaml is gitignored — updated locally but not committed
- Files changed: `docker-compose.yml`, `docker-compose.dev.yml`, `mock/feeds.yaml`
- **Learnings:** `config/` is gitignored — don't try to commit it
---

## [2026-03-24] - #20 Create separate Dockerfiles for subscriber and generator services
- Removed hardcoded CMD; added deprecation warning to __main__.py
- Files changed: `Dockerfile`, `src/__main__.py`
---

## [2026-03-24] - #19 Refactor generator+server into a service that polls .updated signal
- Created generator_service.py and run_generator.py
- Server runs in daemon thread; poll loop checks for .updated signal file
- Files changed: `src/generator_service.py`, `src/run_generator.py`
---

## [2026-03-24] - #18 Refactor subscriber into a looping service with .updated signal
- Modified subscribe.main() to return bool; created subscriber_service.py and run_subscriber.py
- Files changed: `src/subscribe.py`, `src/subscriber_service.py`, `src/run_subscriber.py`
---

## [2026-03-24] - #17 Add interval config to config.py with env var overrides
- Added `_parse_interval()` supporting s/m/h suffixes and compound values (e.g. 2h30m)
- Added `subscribe_interval()` and `generate_interval()` with YAML > env var > default priority
- Updated `DEFAULT_CONFIG` with interval keys
- Files changed: `src/config.py`
- **Learnings for future iterations:**
  - Pre-existing test failures (7 total) are unrelated to this milestone's changes
  - Python 3.14 in system; pydantic_core can't be installed; skip it, not needed for core logic
  - `config.file()` returns a plain dict — pass it directly to helper functions
---
